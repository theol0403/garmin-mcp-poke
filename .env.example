# Garmin authentication (use generate_tokens.py to create this)
GARMINTOKENS_BASE64=

# Fallback: direct credentials (only works for accounts without MFA)
GARMIN_EMAIL=
GARMIN_PASSWORD=

# Server config (set by Render automatically)
PORT=8000
HOST=0.0.0.0
